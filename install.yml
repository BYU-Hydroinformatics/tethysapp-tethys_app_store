version: 1.2.0
tethys_version: ">=4.0.0"
name: app_store

requirements:
  conda:
    channels:
      - conda-forge
    packages:
      - pygithub
      - gitpython==3.1.18
      - requests
      - semver

post:
  - setup_tethys_channel.sh
